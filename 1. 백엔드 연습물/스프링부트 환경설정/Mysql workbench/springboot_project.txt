CREATE DATABASE IF NOT EXISTS database_study;
USE database_study;

CREATE TABLE orders(
	order_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    product_name VARCHAR(255) NOT NULL,
    order_date DATETIME NOT NULL
);